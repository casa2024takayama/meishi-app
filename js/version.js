/**
 * アプリのバージョン情報。
 * リリース時はここと index.html の以下2か所を同じ番号に更新すること:
 *   1. <span class="app-version-badge">vX.Y.Z</span>（ヘッダー表示・更新確認用）
 *   2. <script ... src="js/app.js?v=N">（JSキャッシュ更新用に N を +1）
 */
export const APP_VERSION = 'v2.0.0';
